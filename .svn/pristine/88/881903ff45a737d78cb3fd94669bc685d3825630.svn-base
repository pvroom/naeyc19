import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AttendeemessagePage } from './attendeemessage.page';

describe('AttendeemessagePage', () => {
  let component: AttendeemessagePage;
  let fixture: ComponentFixture<AttendeemessagePage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AttendeemessagePage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AttendeemessagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
