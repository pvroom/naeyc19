import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ConferencecityPage } from './conferencecity.page';

describe('ConferencecityPage', () => {
  let component: ConferencecityPage;
  let fixture: ComponentFixture<ConferencecityPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ConferencecityPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ConferencecityPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
