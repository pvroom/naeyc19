import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ActivityfeedleaderboardPage } from './activityfeedleaderboard.page';

describe('ActivityfeedleaderboardPage', () => {
  let component: ActivityfeedleaderboardPage;
  let fixture: ComponentFixture<ActivityfeedleaderboardPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ActivityfeedleaderboardPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ActivityfeedleaderboardPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
