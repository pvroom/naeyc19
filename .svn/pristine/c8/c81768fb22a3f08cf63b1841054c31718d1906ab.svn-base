import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { Listinglevel1Page } from './listinglevel1.page';

describe('Listinglevel1Page', () => {
  let component: Listinglevel1Page;
  let fixture: ComponentFixture<Listinglevel1Page>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ Listinglevel1Page ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(Listinglevel1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
